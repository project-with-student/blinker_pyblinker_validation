from setuptools import setup

setup(name='blinker_pyblinker_validation"',
      version='0.0.1',
      description='faq',
      author='rpb',
      packages=['src'],
      install_requires=['seaborn','mne']
      )
